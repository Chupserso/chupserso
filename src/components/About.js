const About = () => {
    return (
        <div className="about" data-aos="fade-up">
            <h2 className="app-title">About Me</h2>
            <p>I’m a frontend developer who writes clean, optimized code and builds responsive, animated websites with a focus on performance and detail. I take on projects with full commitment — from idea to final result{/* .<br/>Tech stack: HTML/CSS, React, Typescript, Javascript, SCSS, PHP, MySQL */}</p>
        </div>
    );
}

export default About;